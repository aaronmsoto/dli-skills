# loop: M6-D design brief for Lola la Lechuza · 2026-07-07 · loop/20260707-mascot-d

- Goal criteria addressed: "D (design): design brief in docs/MASCOT.md…"
  (gate waived by owner; brief auto-merges and implementation proceeds).
- What was done: binding spec added to docs/MASCOT.md — barn-owl character
  spec with SVG structure (head/body/sparkles groups, neck transform
  origin), light/dark palette tokens, 7-state pose/animation table
  (is-idle/watching/hop/curious/turn/spin/celebrate) with durations and
  reduced-motion static poses, per-screen placement map (progress-bar
  perch + nest on play screens; no mascot on study/report/print), copy
  lines, per-round journey decision, and the I1/I2 tech contract
  (createLola(size) → {el, setState}; class-swap-only states; ≤15 KB).
- Validation: npm test 40/40 · docs-only; CI on the PR.
- Decisions & rationale: head spin (owner's signature-move request) is the
  single sanctioned >600 ms animation, 3-star results only, once; the miss
  state is deliberately the smallest motion so corrective text wins
  attention; nest ("nido") beats "casa" for an owl and gives the progress
  bar a destination.
- Dead ends / gotchas: remember `transform-box: fill-box` for SVG group
  transforms or head rotation origins break in some browsers.
- Next suggested step: I1 — implement js/mascot.js + CSS states; integrate
  home hero + results screens; extend e2e for state classes.
