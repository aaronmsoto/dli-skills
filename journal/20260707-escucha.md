# loop: M3 🎧 Escucha — recognition-only listening mode · 2026-07-07 · loop/20260707-escucha

- Goal criteria addressed: all four M3 checkboxes (owner decisions applied:
  recognition-only v1, badges-not-stars; typed variant deferred with SME).
- What was done: LISTEN mode outside MODES (star math/sampling untouched by
  construction); voice-gated card + route guard (redirects voiceless
  devices to the group screen); prompt speaks the form (never shows it —
  only infinitive + gloss), 🔊 replay + 🐢 slow (speak() gained a rate
  param, 0.65); listening prompts bypass mute; distractors reuse
  buildChoices (person endings + stress/tense contrasts for free); badges
  (🎧, same tiers) under <set>.<tense>.listen shown on set/home/results/
  review/report, excluded from all totals; report gained a 🎧 column.
- Validation: npm test 42/42 · e2e 23 checks PASS — full round played "by
  ear" via the stubbed voice (spoken text drives the clicks, 10/10 → 3
  badges), slow-replay rate asserted, prompt structure asserted not to
  reveal person/form, home card shows "🎧 3/9" while stars stay 0/30,
  voiceless gating both ways (card hidden + route redirect).
- Decisions & rationale: badges deliberately fail-open display (earned
  badges show even if the voice later disappears — earned is earned);
  results "next mode" suggestion falls through to Elige, which is fine.
- Dead ends / gotchas: e2e stub now records {text, rate} objects — any
  future spoken-text assertion must use .text.
- Next suggested step: M2 sentence bank remains ON HOLD for SME; typed
  Escucha variant queued behind the same SME conversation. M4 (near-future
  & progressive tenses) is the next unheld milestone.
