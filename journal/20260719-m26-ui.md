# 2026-07-19 — M26.2: stretch-tense UI (loop/20260719-m26-ui)

## What

Second M26 iteration — the Estudia/Práctica surfaces for the two stretch
constructions, strictly unscored-first:

- **Set screen** — new section "4 · Tiempos nuevos" with two cards
  ("Muy pronto ⏭️" / "Ahora mismo 🔄" — the GOAL's exact framing),
  linking to Estudia only, labeled "sin estrellas · unscored", never a
  starRow. They reuse .mode-card styling but are excluded from the
  activity-card e2e counts via `:not(.stretch-card)` (they are not
  activities).
- **Routes** — #/study and #/practica accept nearfuture/progressive;
  #/play deliberately does NOT (e2e asserts the bounce to home).
- **Estudia** — full phrase tables ("voy a hablar", "estoy diciendo")
  via `formsFor`; hint/example from STRETCH_META; action row lists
  Práctica + print ONLY (rule 2: the row is ALL activities for the
  tense — scored games don't exist for stretch). Print works via the
  existing conj-table print styles.
- **Práctica** — full rebuild works on phrase tiles (buildPracticaBank
  extended in game.js); same sticky-hover guards, no stars/hints.
- **Audio gating** — new `hasClip(text)` export (audio.js) +
  `stretchSpeakable` probe: stretch clips don't exist until the owner's
  ElevenLabs run, so their 🔊 affordances gate on TTS-or-manifest-probe,
  never `audioAvailable()` alone (a silent button would break rule 1).
  Voiceless parity asserted in e2e.
- **Icons** — two new tense line icons (forward-arrow-to-bar, cycle) in
  the M17 24×24 2px-stroke mask language, both `--brand` leaf green.

## Verification

- e2e: new M26 block — 2 unscored cards, RAE phrases asserted in both
  study tables (incl. irregular diciendo/pudiendo/viendo), Práctica
  phrase-tile placement, no play links/routes, voiceless parity. The 3
  activity-count assertions gained `:not(.stretch-card)`.
- prado screenshot loop: set (light+dark), study progressive, 360px
  nearfuture all reviewed (line icons render, persons column frozen).
- unit 66/66; suites green.

## Next

M26.3: tools/generate-audio.mjs --stretch extension, SPEC/STANDARDS/
about docs, close M26 (audio run + star-grid decision stay owner boxes).
