# 2026-07-08 — M12 complete: pre-generated Spanish clips + dual audio backend

**Branch:** `loop/20260708-m12-clips`

## What shipped

- **~6,400 clips** (audio/clips/*.mp3 + manifest.json): every dataset
  text in two text-variants (person-prefixed for say/sayForm, bare for
  🎧 Escucha) × two dual-generated speeds (🔊 0.85 / 🐢 0.70) — plus
  "Hola" for the unmute greeting. Voice `rixsIpPlTphvsJd2mI03`, model
  eleven_multilingual_v2, mp3_22050_32. **Owner audition loop worked
  through three rounds**: normal-pace samples → 20% slower (0.8) →
  final decision: dual speeds matching the UI's 🔊/🐢 buttons.
- **js/audio.js dual backend**: clips first (exact-spoken-text lookup;
  🐢 selects the real 0.70 recording), Web Speech fallback,
  `audioAvailable()` = either. app.js gates all audio UI on it and boots
  via `initClips().finally(render)`. **🎧 Escucha now works on
  voiceless-but-online devices** (school Chromebooks).
- **Privacy claims stay true**: clips download like images; no text
  leaves the device; no runtime ElevenLabs calls; key lives only in the
  git-ignored .env. Docs updated everywhere the old "built-in voice"
  wording lived (SPEC 4.3c, about.html, docs/index.html, README,
  CLAUDE.md incl. standing rule 1 → gate on audioAvailable()).
- **Tests**: new tests/audio.test.mjs (manifest covers the full dataset,
  both variants, no dead entries). E2e: the main and voiced contexts now
  route-abort the manifest so every existing "voiceless"/Web-Speech
  assertion keeps its exact meaning; a new clips context (Audio stubbed,
  no speechSynthesis) asserts clip playback ("yo soy" src match, rate 1),
  Escucha card + 7 activity cards voiceless-online, bare-form prompt
  clips, 🐢 rate 0.5, and mute silencing clips. .crumbs now wraps
  (sound toggle appears on more devices at 360px).

## Ops notes

- `pkill -f generate-audio` matched the invoking shell too (exit 144) —
  harmless but noted for future loops.
- Credits: ~10k spent across two discarded partial runs (normal pace,
  then single 0.8) + ~60k for the final dual-speed set — inside
  Creator's 100k monthly allowance with ~30k headroom.

## M12: R1 ✓ G ✓ I ✓ V/RT ✓ — complete. Queue IDLE.
