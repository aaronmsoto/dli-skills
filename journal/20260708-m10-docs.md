# 2026-07-08 — M10 P+D+V/RT: public /docs pages + axe-core CI gate (M10 loop items done)

**Branch:** `loop/20260708-m10-docs`

## What shipped

- **docs/index.html** — public documentation hub at `/docs/`: how to use
  the app (learners/parents/teachers: privacy/localStorage, voice
  availability, printing, review queue), DLI standards support (named
  indicators + links to both standards documents + STANDARDS.md), and
  usability & accessibility (links the report page + all four audits).
  Self-contained styling (light/dark), relative links only, unlinked from
  app navigation per owner decision.
- **docs/usability.html** — the Usability & Accessibility page inside
  /docs: methodology, the full findings tables with FIXED/OWNER-TRIAGE
  status per finding ID, the honest screen-reader-pass limitation, and
  audit date.
- **axe-core CI gate** — vendored `tests/e2e/vendor/axe.min.js`
  (MPL-2.0 banner retained); the e2e suite injects it and fails on ANY
  critical/serious violation across 7 representative routes. It caught a
  real one immediately: `aria-label` on the plain streak `<span>`
  (aria-prohibited-attr, serious) — fixed with `role="img"` in both play
  and contrast headers. Lesson: `npm i --no-save axe-core` mid-run
  PRUNES playwright (no-deps package.json) — hence vendoring, not
  self-install; recorded in CLAUDE.md.
- E2e also verifies both /docs pages: titles, all three hub sections,
  ≥5 FIXED + exactly 2 owner-triage rows, and every relative link
  resolving (HTTP 200).

## M10 status

A1-A4 ✓ · F ✓ · P ✓ · D ✓ · V/RT ✓ — all loop items done. Open, owner
only: NN-1 toggle-restart and NN-3 start-here-cue triage (options +
recommendations in GOAL.md). Queue is IDLE.

## Validation

`npm test` 46/46 · `npm run e2e` PASSED (axe gate green on 7 routes).
