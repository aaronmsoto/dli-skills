# 2026-07-19 — M25 polish: apple-touch-icon + manifest id (loop/20260719-m25-icon-polish)

## What

Owner-approved follow-up closing the PWA-asset audit gaps (owner kept
the light-theme preference — theme_color stays the single Prado light
green, no dark-variant meta):

- **icons/apple-touch-icon.png** (new, 180×180 FULL-BLEED) — iOS
  composites transparency onto black, so the home-screen icon must not
  have the rounded icons' transparent corners; iOS rounds corners
  itself. Mark at 1.1 scale keeps the star spark clear of the ~20%
  system corner radius. Generated by the same one-shot
  tools/generate-icons.mjs (regeneration was byte-identical for the
  three existing PNGs — the renderer is deterministic).
- **index.html** — apple-touch-icon link now points at the dedicated
  file instead of the transparent-cornered icon-192.
- **manifest.webmanifest** — `"id": "./"` (stable app identity if
  start_url ever changes).

Skipped deliberately (recorded in the M25.4 report): manifest
screenshots, iOS splash images, 192 maskable variant, dark theme-color
meta (owner preference: light).

## Tests

e2e M25.1 block extended: apple-touch-icon link present, the PNG
resolves with magic-byte check, manifest `id === "./"`. Unit 60/60
(manifest still in the payload budget).
