# 2026-07-08 — GOAL.md: M12 premium-audio epic written, ON HOLD (owner inputs)

**Branch:** `loop/20260708-goal-m12-audio` · docs-only

Captured the ElevenLabs analysis as **M12** with the owner-selected
architecture: pre-generated static clips (owner-run local script,
`ELEVENLABS_API_KEY` env-only, never shipped/committed/CI), zero runtime
third-party calls so the privacy claims stay true. Phases: R1 voice
audition (feeds the owner's voice choice + license check), G generation
tooling (~1,800 person-prefixed clips, idempotent, manifest-tested),
I integration (backend chain clip → Web Speech → hidden; despacio via
playbackRate; Escucha unlocks on voiceless-but-online devices), V/RT
(stubbed network audio, fallback-order + gate assertions, honest privacy
wording). Hard hold recorded: loops must NOT start until the owner
provides (1) the API key as a local env var and (2) voice candidates or
a chosen voice. Queue line updated accordingly.

Validation: docs-only; `npm test` green.
