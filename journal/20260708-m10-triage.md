# 2026-07-08 — M10 triage fixes: NN-1 (option b) + NN-3 (option a); M10 CLOSED

**Branch:** `loop/20260708-m10-triage` · owner decisions 2026-07-08

## NN-1/DN-4/CW-3 — mid-round toggles no longer restart the round

Owner chose option (b): during an active round (play/contrast/práctica),
footer toggles save the setting, show "✓ Guardado — se aplica al
continuar" (role=status), and do NOT re-render. Hints apply on the next
question (renderQuestion re-reads settings); vosotros applies at the next
round/table. Off-round screens re-render immediately as before.

## NN-3/DN-1/CW-1 — "¡Empieza aquí!" ribbon

Owner chose option (a): the first group with zero stars AND zero badges
wears a brand-colored ribbon on the home grid; it advances as groups get
started and disappears when none are fresh. No new stored state.

## Also

- docs/usability.html: both findings flipped to FIXED with dates — the
  public scoreboard now shows zero open findings (screen-reader pass
  still flagged as the known limitation).
- GOAL.md: both triage boxes checked with the chosen options recorded;
  **M10 marked complete**; queue remains IDLE (human-only items left).
- SPEC 4.3j updated + new 4.1b.

## Validation

`npm test` 46/46 · `npm run e2e` PASSED — new checks: fresh context shows
exactly one ribbon on Grupo 1; seasoned context moves it to Grupo 2;
mid-round hints-toggle preserves progress-text and applies at the next
question; off-round vosotros toggle still re-renders (5↔6 rows);
usability-page assertions now require ≥7 FIXED and zero pending.
