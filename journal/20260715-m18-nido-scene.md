# loop: M18.2a El Nido de Lola — scene, route, derivation · 2026-07-15 · loop/20260715-m18-nido-scene

- Goal criteria addressed: first half of "M18.2 — El Nido: `#/nido` scene
  (new module `js/nido.js`, NOT in mascot.js), tiered growth derived from
  `best`, never empty slots/counters, DOM-list a11y" (checkbox stays open —
  M18.2b adds celebrations + home-card twig badges + the demo flag).
- What was done:
  - `js/nido.js` (new, ~4 KB): pure `nestTier()` (wisp = first star, twig =
    every star activity ≥1★ — NO perfection gate, flor = 30/30 upgrade),
    `nestSummary()` (invitation grammar, never a deficit count), and
    `createNido()` — semantic focusable list FIRST, decorative procedural
    SVG scene (one twig path template rotated per item, aria-hidden),
    tap-to-hear via injected `speak` only when `canSpeak`.
  - `js/app.js`: `#/nido` route + title + render case; `nestItems()`
    derivation (TENSES×MODES + contrast, all from existing `best` — zero new
    storage); home hero "El nido de Lola" pill link; standard chrome
    (crumbs, ☰, ℹ️ via new STANDARDS_INFO.nido entry, footer).
  - `css/redesign.css`: nest line-icon mask (M17 icon-language compliant),
    nido card/list/scene styles, 44px+ items.
  - Unit tests: tier derivation table + summary grammar (caught a real
    double-count bug: a flowered twig was counted as both ramita and flor).
  - e2e "M18.2a nido" blocks: empty state inviting + counter-free, 3-tier
    seeded derivation, aria-hidden scene, home link, voiceless context
    renders plain items with zero audio affordances.
- Validation: npm test 51/51 (incl. the new nido test + standards-info key) ·
  e2e PASS incl. both new nido blocks · screenshots m18-nido.png.
- Decisions & rationale: `nido.js` keeps its own 6-line `h()` helper instead
  of exporting `el()` from app.js (entry module must stay import-free of
  UI modules); tier names are la brizna / la ramita / la flor — noun audio
  rides Web Speech until the owner's ElevenLabs clip run (M18 owner
  sign-off list).
- Dead ends / gotchas: stubbing `speechSynthesis` as `undefined` in e2e
  crashes audio.js's boot (`window.speechSynthesis.addEventListener?.()` —
  the optional chain is on the method, not the object); stub a voiceless
  OBJECT instead. `browser.newPage()` = fresh storage context in this suite.
- Next suggested step: M18.2b — showResults crossing detection (wisp/twig/
  flor ceremonies), home-card twig badges, `?m18demo=1` flag, about.html note.
