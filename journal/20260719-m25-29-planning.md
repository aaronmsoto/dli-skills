# Next-era planning: M25-M29 queued · 2026-07-19 · claude/github-repo-context-status-q2u4i8

Owner-directed planning session (plan mode; owner approved the plan).
Request: milestones for (1) anonymous analytics, (2) optional cross-device
accounts, (3) free/portable DB backend, (4) PWA + offline audio, (5) my
pick — with orchestrated research and adversarial refinement.

## Process

- Repo-facts brief (Explore agent): shell 59 KB gz; audio 39.4 MB total,
  ~2 MB/set; progress blob 21 KB with ZERO PII; verbatim inventory of every
  privacy promise items 1-3 would break — incl. the load-bearing
  "COPPA/FERPA-friendly by design" cite in standards-info.js; e2e/SW
  hazards; `?m18demo=1` querystring constraints.
- Design agent with web research + self-adversarial critique: COPPA 2025
  amendments ("support for internal operations" exception permits aggregate
  analytics on child-directed sites WITH notice disclosure; IPs/persistent
  identifiers are personal information under 16 CFR 312.2); privacy-first
  analytics landscape; sync-auth options (Google OAuth rejected for
  children; anonymous sync codes designed instead); backend vendor
  free-tier forensics (Supabase idle-pausing disqualifier; Heroku/Railway/
  PlanetScale rug-pull history; Cloudflare D1 = portable SQLite); iOS PWA
  storage quotas (17+: origin quota ≈60% of disk; persist() heuristic for
  Home-Screen apps); SW strategy for a no-build/no-hash site
  (network-first shell beats SWR to avoid version skew).

## What landed

- GOAL.md: queue line → M25 ACTIVE; entries M25 (PWA+Descargas, no
  amendment needed), M26 (M4 reactivated unscored-first — owner scope via
  plan approval; star-grid question parked as a separate owner decision),
  M27 (anonymous sync codes — BLOCKED on the owner signing the embedded
  privacy-amendment text + Cloudflare setup; loops may not touch network
  code before the checkbox), M28 (aggregate beacon on the same Worker —
  same gate), M29 (teacher mode: class packs + QR deep links). M-PLAN
  record entry with evidence + EXCLUSIONS (GA4, Firebase, CF Web
  Analytics, Plausible/Umami hosted, Supabase, passwords, OAuth-for-kids,
  push notifications, Workbox, typed-Escucha). Stale M16-era queue
  narrative removed; original M4 entry marked reactivated-as-M26.
- docs/SPEC.md §5.5-5.7: forward designs (SW strategy + Descargas; sync
  model incl. per-key-max merge matching recordResult semantics, sha256'd
  codes, 40-bit 4-word Spanish codes, 180-day expiry, no-IP storage;
  beacon schema + no-op guards) so loops implement ONE design.

## Validation

npm test 56/56 (docs-only). Heading-integrity check: M0-M29 present exactly
once (M22-incident discipline). e2e not required (no UI change).

## For the next loop

M25 ICONS+MANIFEST is the first loop-workable item. Load prado-visual-craft
for the icon work. Implement the SPEC §5.5 design — do not re-derive it.
