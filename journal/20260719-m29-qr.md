# 2026-07-19 — M29.2: 📱 QR stations + teacher landing; M29 shipped (loop/20260719-m29-qr)

## What

Final M29 iteration:

- **tools/vendor/qrcode-generator.cjs** — vendored MIT encoder (Kazuhiko
  Arase, same pattern as the vendored axe). `.cjs` because the repo is
  `"type": "module"` (a `.js` require resolves as ESM and yields an
  empty namespace — the gotcha that broke the first run).
- **tools/generate-qr.mjs** — one-shot generator; **qr/g01-g20.svg**
  committed (scalable SVGs, EC level M, linking to the LIVE
  https://dliskills.com/#/set/<n> so printed cards work anywhere).
- **Pack page** — leads with a 📱 station card (QR + bilingual caption,
  its own printed page), so one print job now yields station card +
  five blank sheets + five answer keys.
- **about.html** — "Para maestros / For teachers" landing section
  (go-live content rule); README teacher-mode bullet; SPEC §4.3n.
- **GOAL** — M29 marked shipped; queue line: the approved M25-M29 plan
  is fully shipped, no active loop milestone, M28 deploy-ready on its
  vendor gate alone.

## Verification

- e2e: M29 block extended — station QR present, qr/g01.svg resolves as
  SVG, sheet counts exclude the station card, about.html teacher
  section asserted. Suites exit-code-verified green.
- Owner action open: print-test a pack on paper + scan a QR with a real
  tablet.
