# loop: M18.1 Empareja con Chispa + F1 droplet · 2026-07-15 · loop/20260715-m18-chispa

- Goal criteria addressed: "M18.1 — Empareja con Chispa + freebie F1
  (Repasa-hoy droplet visual)" (owner-approved go-live target, 2026-07-15).
- What was done:
  - `js/audio.js`: new `chirp(kind)` — synthesized WebAudio oscillator blips
    ("match" = two rising sines, "run" = three), lazy AudioContext created
    inside the tap call chain (iOS unlock), fail-safe try/catch, no files.
  - `js/app.js` renderMatch: up-only `.pareja-count` ("N parejas", starts
    empty), internal `run` counter of consecutive first-try matches —
    celebrated in the feedback line when ≥3 ("¡3 seguidas!"), NEVER displayed
    as a reset (a visible reset would be the app's first punishment
    mechanic); chirp gated on the sound setting; miss path unchanged.
  - `css/redesign.css`: pick-lift (translate/scale/rotate + shadow-pop),
    match-settle `chispa-pop` keyframe, counter/water styles — all decorative,
    all stripped under `prefers-reduced-motion` (state classes stay
    synchronous, so existing e2e timing contracts hold).
  - F1: `.review-water` line on the home review queue — "Un poquito de repaso
    riega lo aprendido 💧" — the garden metaphor as invitation; deliberately
    no backlog count (e2e asserts no digits in the line).
  - `about.html`: new "Game design, grounded in research" section (Deci,
    Zendle, Duolingo-streak study, Fairplay/Prodigy, NN/g motor skills) per
    the owner's go-live directive; fixed a stray ⭐🌙 in the tense row that
    M17's icon cleanup missed.
- Validation: npm test 50/50 · e2e PASS incl. new "M18.1 chispa" block
  (counter empty at start, unchanged on miss, up-only through the solve,
  "seguidas" celebration, chispa-pop present and removed under reduced
  motion, watering line digit-free) · screenshots in shots/.
- Decisions & rationale: the deliberate-miss e2e step targets the LAST
  pair's right card so exactly one solved pair is non-first-try and the run
  deterministically reaches 3+ regardless of shuffle (first version was
  flaky — random wrong card could break the run late).
- Dead ends / gotchas: results render 700 ms after the final match tears
  down the board — style assertions must run before the last pair is
  matched. `browser.newPage()` = fresh context/storage in this suite.
- Next suggested step: M18.2 Nido scene iteration (js/nido.js + #/nido).
