# loop: M6-I2 Lola in the games · 2026-07-07 · loop/20260707-mascot-i2

- Goal criteria addressed: "I2 (implementation): in-game integration — owl
  travels the round's progress path; reacts to correct (hop) and incorrect
  (curious tilt, never negative); 3-star celebration." (3★ spin shipped in
  I1's results screen; this wires the in-round journey.)
- What was done: Lola (46px) perches on the progress bar at the fill edge
  and glides right as questions complete (left % + CSS transition); twig
  nest SVG (createNest) marks the goal; helping copy "¡Ayuda a Lola a
  llegar a su nido!" on Elige/Escribe/Contrast; correct → is-hop (is-turn
  at streak ≥3), wrong → is-curious, new question → idle; Escribe input
  focus → is-watching (still, eyes down); Empareja shows Lola beside the
  title with a look-back turn per matched pair.
- Validation: npm test 40/40 · e2e PASS with new assertions: perch+nest+
  copy present on play screens, is-watching on input focus, is-hop after a
  correct submit, is-curious observed on wrong answers, is-turn on a
  matched pair (first pair driven via UI), mobile 400px screenshot clean.
- Decisions & rationale: perch positioned by the same % as the fill so the
  two can never disagree; .play-lola is pointer-events:none and lives in
  the header band so it can't cover controls.
- Dead ends / gotchas: keep .progress-bar overflow:hidden for fill
  rounding — perch must live in a sibling absolute layer (bar-holder),
  not inside the bar.
- Next suggested step: V+RT — payload budget check, dark-mode + reduced-
  motion screenshots, full regression sweep, docs sync (SPEC/README/about).
