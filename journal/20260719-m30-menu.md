# 2026-07-19 — M30.2: ☰ hamburger overhaul (loop/20260719-m30-menu)

## What

Owner-directed menu redesign, per the WAI-ARIA APG disclosure-navigation
pattern (Tab navigation — site nav, not role=menu):

- **Text-only rows** (owner: no icons): Inicio, Informe, Descargas,
  Instalar la app, Acerca de, Documentación, Ajustes.
- **Ajustes / Settings** — expandable sub-group (aria-expanded +
  aria-controls, ▾/▴ caret): Sonido, Tema (Auto/Claro/Oscuro), Pistas,
  Vosotros, Borrar progreso. Mirrors the footer via the same
  mid-round-safe applySetting semantics (footer unchanged — owner
  chrome). Sound/theme keep their classes so every existing e2e flow
  still drives them (each flow now expands Ajustes first).
- **Visual scrim** — dims the page while open; deliberately
  pointer-events:none so the document-level closer keeps the
  established outside-click semantics (and Playwright clicks aren't
  intercepted).
- **Dialog handoff** — the install row CLOSES the menu before its
  dialog opens, and `.info-overlay` moved z-index 40 → 70 (it was UNDER
  the menu's 45 — the owner-reported bug). Panel 45 > scrim 44.
- **Focus** — open → first row; close by ANY path (Escape, outside
  click, row navigation) → focus returns to ☰; aria-expanded on both
  the button and the Ajustes group stays truthful.
- 44px min rows, panel max-height + scroll, no new motion (reduced-
  motion parity is trivial).

## Verification

- e2e: existing menu/theme/sound flows updated for the Ajustes level
  (7 theme + 2 sound call sites); the site-menu block gains a no-emoji
  regex assertion + settings-group presence; NEW M30.2 block — scrim
  visible & visual-only, settings collapsed→expand→hints toggle
  actually persists, install dialog closes the menu and
  `elementFromPoint` proves it is the topmost layer; Escape dismisses.
- Suites exit-code-verified green (unit 74/74, full e2e).
- prado screenshot loop: menu light/dark, Ajustes expanded, 360px,
  dialog-over-page — all reviewed.

## Next

M30.3: axe scans of the new states + keyboard pass + SPEC §4.3k
rewrite; close M30.
