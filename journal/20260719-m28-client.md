# 2026-07-19 — M28.2: client beacon + disclosure; M28 shipped (loop/20260719-m28-client)

## What

- **js/beacon.js** — pure `beaconAllowed` guard (no sendBeacon API,
  localhost/127.*, webdriver, `conjuga.noSW`, Global Privacy Control)
  + fire-and-forget `pageView`/`feature` via sendBeacon (text/plain →
  no preflight). One ENDPOINT constant (workers.dev; a branded domain
  later is a one-line change). Analytics can never break the app:
  every path guarded and wrapped.
- **Wire-up**: `pageView(route.screen)` in render(); `dl` on a
  completed Descargas group download; `install` on opening the install
  panel — the exact event set named in SPEC §5.7.
- **sw.js** — beacon.js joins the shell; VERSION → shell-v3 (beacon
  requests are cross-origin, so the SW passes them through untouched).
- **Disclosure** (owner-approved uniques addendum) applied verbatim to
  about.html Privacy, GOAL invariant 2, CLAUDE.md rule 2, STANDARDS §4;
  SPEC §5.7 rewritten as shipped (events, uniques design, live URL,
  explicit non-goals: no sessions, no all-time uniques);
  server/README.md carries the live URL + dashboard/query commands.
- **GOAL**: CLIENT + V boxes; M28 marked shipped; M26 star-grid
  decision recorded (owner: stretch STAYS unscored — do not
  re-propose); queue line final.

## Verification

- unit: beaconAllowed truth table (each guard independently blocks);
  beacon.js inside the payload budget via the js/ glob.
- e2e: new M28.2 block — a webdriver context navigating home→set and a
  webdriver-hidden GPC context both fire ZERO requests to */beacon
  (route-intercepted counter). Positive path was live-verified in
  M28.1 (localhost guard precludes an in-harness positive by design).
- Suites exit-code-verified green; GOAL M0-M29 integrity OK.

## Production note

Real collection starts only when the owner merges the release PR
containing this client (notice already precedes collection: the
amended about.html text shipped with #104/#116).
