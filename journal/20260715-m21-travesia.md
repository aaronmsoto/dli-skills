# loop: M21 La Travesía — the flight becomes a journey · 2026-07-15 · loop/20260715-m21-travesia

- Goal criteria addressed: all of M21 (A/B/C + V), owner pick 2026-07-15
  ("Do a b c") after the design question in chat.
- What was done:
  - **A (traversal):** journey strip in js/vuelo.js — one puff per prompt,
    the nest at the right, Lola positioned by `--vuelo-progress`; each
    correct answer fills a puff and flies her one step home (CSS left
    transition + swoop arc under no-preference; instant reposition under
    reduce). Strip is decorative/aria-hidden; meaning stays in the live
    region and the n/6 text. Landing = arrival.
  - **B (listen-first):** `listenMode = !!onSay`; app.js now passes onSay
    only when `audioAvailable() && settings.sound` — with it, the written
    prompt hides (🎧 cue + prominent 🔊 replay) and the flight becomes an
    ear game distinct from Elige; the ABC toggle (aria-pressed) reveals the
    text in one tap (equity escape hatch for deaf/HoH learners; not
    persisted). Muted/voiceless contexts default to text with no dangling
    audio affordances.
  - **C (atmosphere):** `.vuelo-skyband` warms morning→midday→sunset via
    `data-step` color-mix stages on Prado tokens (dark theme automatic);
    two parallax cloud bands drift behind the anchored targets. Every
    moving/transitioning rule lives inside the no-preference query.
  - about.html flight row updated (journey + by-ear + ABC).
- Validation: npm test 52/52 · e2e PASS — new M21 block (listen-first
  default, ABC toggle both directions, data-step/puff/--vuelo-progress
  advancement, landing at step 6, toggle retires at landing; muted context
  gets text + no toggle/replay) and all prior vuelo blocks still green
  (prompt textContent stays set even while visually hidden, so existing
  solvers were unaffected).
- Decisions & rationale: listen-first engages only when sound can actually
  play — passing onSay while muted would have made the ear game silently
  unwinnable; the sticky-hover guard and anchored-target rules carry over
  untouched.
- Dead ends / gotchas: none new; remember `.vuelo-prompt` hides via class
  but keeps textContent (e2e relies on it).
- Next suggested step: none loop-workable — owner merges the rolling
  release (M19+M20+M21); M18.4 Postales still SME-blocked.
