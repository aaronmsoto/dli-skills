# 2026-07-19 — M26.3: audio tool + docs; M26 shipped (loop/20260719-m26-close)

## What

Final M26 iteration — the audio pipeline extension and the go-live
documentation, closing the milestone:

- **tools/generate-audio.mjs** — new `--stretch` flag: generates the
  1,200 person-prefixed stretch phrases ("yo voy a hablar",
  "ellos están diciendo") × 2 speeds ≈ 2,400 clips. Person-prefixed
  ONLY — there is no stretch Escucha, so no bare variants. Owner action
  recorded in GOAL: run it with the ElevenLabs key, commit audio/, and
  the stretch 🔊 buttons light up automatically via the `hasClip` probe.
- **about.html** — activities table gains the ⏭️🔄 Tiempos nuevos row
  (exposure-first, deliberately starless), the stretch note joins the
  Intermediate-expectation caveat, and a new **Linguistic accuracy**
  paragraph publicly documents the RAE verification practice (owner ask
  2026-07-19: RAE was previously cited only in README/SPEC, not on any
  learner/parent-facing page).
- **docs/index.html** — bilingual "Precisión lingüística" paragraph
  (RAE + stretch constructions) on the public help page.
- **docs/STANDARDS.md** — Tiempos nuevos row with the honest framing:
  unscored by design; the RAE-verified engine guarantees the *models*
  are correct, not that mastery is measured.
- **docs/SPEC.md** — §4.3m records the full stretch design (engine
  isolation, pret3-derived gerunds, per-text audio gating, the later
  star-grid owner decision).
- **GOAL.md** — AUDIO (loop side) + V checked; explicit open OWNER
  ACTION box for the --stretch run; M26 marked shipped; queue advanced
  (M29 next on the owner's go; history M0-M26).

## M26 wrap-up

Three iterations (engine → UI → close), PRs #111/#113/#114, no CI reds.
Open owner boxes: the --stretch ElevenLabs run, and the later star-grid
decision (never a loop's call).
