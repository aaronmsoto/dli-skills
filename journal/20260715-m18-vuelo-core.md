# loop: M18.3a El Vuelo de Lola — core flight · 2026-07-15 · loop/20260715-m18-vuelo-core

- Goal criteria addressed: first half of "M18.3 — El Vuelo" (checkbox stays
  open; M18.3b adds drift/bob garnish, audio prompts, prefetch, about note).
- What was done:
  - `js/vuelo.js` (new lazy module, ~5 KB raw): `createVuelo({set, tense,
    stars, persons, onSay, onDone})` — 6 prompts sampled from the round's own
    verbs (covert micro-review), 4 anchored ≥64px cloud buttons (5 at 3★ —
    flair scales, access never gates), tap-tap only, NO failure state (wrong
    cloud wobble class, prompt unchanged, never disabled), landing at the
    nest, always-visible Saltar, aria-live narration, role=dialog.
    Reduced-motion-FIRST: the core is fully static; motion arrives in 3b.
  - `js/app.js` showResults: "¡Vuela con Lola!" invitation on every
    star-track results screen (choice/type/match/contrast — not Escucha, to
    keep the 🎧 track's audio identity clean); dynamic `import("./vuelo.js")`
    on tap, offline catch → disabled button with a bilingual message;
    contrast rounds fly in preterite; vosotros persons respected; `onSay`
    injected only when `audioAvailable()`.
  - `css/redesign.css`: overlay (100dvh-safe), sky grid, cloud pills,
    landing; `.vuelo-perched` static under reduced motion.
- Validation: npm test 51/51 · e2e PASS with two new blocks: full flight
  (5 clouds at 3★, ≥64px targets, wrong-tap non-failure, landing, Seguir
  closes, reduced-motion structure identical, Saltar closes) and an
  offline-degrade page (module fetch aborted → button explains itself,
  results intact — run UNTRACKED because the blocked fetch logs an expected
  console error).
- Decisions & rationale: the flight is an on-results INVITATION (one tap in)
  rather than an auto-hijack — satisfies "always skippable" trivially, keeps
  round pacing intact, and stays an unexpected celebration the first time.
- Dead ends / gotchas: keep new module drafts OUT of the repo tree until
  their branch exists (the stop-hook untracked-files check); vuelo advance
  timer is 650ms — e2e waits 720ms per prompt.
- Next suggested step: M18.3b — motion garnish inside a no-preference media
  query, audio prompt on gesture, requestIdleCallback prefetch, about.html
  flight note, GOAL M18.3 checkbox.
