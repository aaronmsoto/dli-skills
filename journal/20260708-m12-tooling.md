# 2026-07-08 — M12 tooling ready; generation BLOCKED on ElevenLabs plan (402)

**Branch:** `loop/20260708-m12-audio-tooling`

## What shipped

- `tools/generate-audio.mjs` — dev-only generator: key from env/.env
  (never committed; `.env` added to .gitignore and verified ignored),
  owner's voice `rixsIpPlTphvsJd2mI03` as default, `--samples` audition
  set / `--sets 1,2` / `--sets all`; two variants per form (person-
  prefixed for say/sayForm, bare for 🎧 Escucha whose prompt must not
  reveal the person); dedupe by exact spoken text (ser/ir "fue" = one
  clip); diacritic-safe filenames with a short hash (hablo ≠ habló);
  per-clip manifest checkpointing (idempotent + resumable); sequential
  with 429/5xx backoff; compact mp3_22050_32 output.

## Verified / found

- Outbound HTTPS to api.elevenlabs.io works from the loop environment;
  the key authenticates and is correctly scoped to TTS (voices_read is
  absent — fine; generation doesn't need it).
- **BLOCKER:** the account is on the Free plan → HTTP 402
  "Free users cannot use library voices via the API" for the chosen
  library voice (also for legacy premade IDs). Zero credits consumed;
  zero clips generated.
- Plan math for the owner: prefixed-only full dataset ≈ 20k credits;
  adding bare Escucha variants ≈ 33k total. Starter ($6, 30k/mo) covers
  it across two runs or prefixed-first; Creator ($22, 100k) covers
  everything in one pass — and paid tiers carry the commercial license
  that distributing the clips on a public site needs.

## Next

Owner upgrades the plan → rerun `--samples` (8 audition clips for
owner/SME listening), then `--sets 1,2` per the owner's "couple groups
first" instruction, then integration (I) and V/RT per M12.
