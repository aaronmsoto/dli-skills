# loop: docs-sync sweep + e2e flake fix · 2026-07-15 · loop/20260715-docs-sync

- Goal criteria addressed: the "docs in sync (SPEC/STANDARDS/about.html/
  README)" clause of the definition of done, owed by M18-M21 (each phase
  updated about.html but not the deeper docs). No feature changes.
- What was done:
  - docs/SPEC.md: new §4.3l (M18-M21 celebration layers — Chispa, Nido,
    Vuelo/La Travesía, shared guardrails, `?m18demo=1` flag), §4.3g Escucha
    rewritten (clips-era availability + M19 accessibility rationale + 🪶),
    §4.4 notes the results-screen flight invite and ceremonies.
  - docs/STANDARDS.md: 🪺/🌤️ rows added to both alignment tables, Escucha
    rows carry "progress never requires hearing", stale ⭐/🌙 icon pair
    removed (M17 rule).
  - README.md: Nido/Vuelo/Chispa features, Escucha rationale + availability
    corrected, stale tense emoji framing fixed, project structure updated
    (mascot/nido/vuelo/standards-info; audio.js description), `npm run e2e`
    documented, roadmap refreshed (Escucha shipped → typed variant;
    Postales listed as vetted-and-waiting).
  - docs/games-proposal.html: status pill + outcome note (Ideas 1+2 SHIPPED
    M18-M21; Idea 3 SME-parked) so the public doc reflects reality.
  - tests/e2e/smoke.mjs: fixed a FLAKY M20-3 assertion — the "no-hover
    clears on movement" step moved the mouse by a fixed offset that could
    exit the sky (listener never fires) depending on which shuffled cloud
    was correct. Now moves to another cloud's center, mirroring the .choice
    grid test's semantics. Assertion strength unchanged.
- Validation: npm test 52/52 · e2e PASSED twice consecutively (flake
  confirmed gone).
- Decisions & rationale: docs describe shipped behavior only; the proposal
  page keeps its full research content with a dated outcome banner.
- Dead ends / gotchas: pointermove listeners scoped to an element only fire
  while the pointer is OVER it — e2e "move away" steps must stay inside.
- Next suggested step: none — queue empty; owner merges Release PR #90+
  (M21 + this sweep); M18.4 awaits SME.
