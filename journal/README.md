# journal/

One file per loop iteration — `<yyyymmdd>-<slug>.md`, template in
docs/LOOP.md. Per-iteration files keep parallel auto-merges into `dev`
conflict-free. Never edit another iteration's file; the human-curated
index and pre-harness history live in /JOURNAL.md.
