# loop: M6-R mascot research + owner redirect · 2026-07-07 · loop/20260707-mascot-r

- Goal criteria addressed: "R (research): extend docs/MASCOT.md with 2-3
  more comparable mascots… and a short motion/a11y note for K-5 attention."
  Plus owner redirect applied to GOAL.md: name = **Lola la Lechuza**,
  design gate waived, head-spin adopted as signature celebration move.
- What was done: comparables added (Duo — borrow archetype, avoid guilt
  mechanics; ClassDojo — classroom-culture integration; Scratch Cat —
  learner-owned neutrality) with borrow/avoid lists; K-5 motion & attention
  notes (≤600 ms feedback, minimal idle, vestibular safety for spins,
  aria-hidden decorative rule, no animation during typing); design
  decisions recorded (barn-owl silhouette distinct from Duo, ~270° realistic
  turns + one full spin only at 3 stars, reduced-motion static fallback).
- Validation: npm test 40/40 · docs-only, no UI surface; CI on the PR.
- Decisions & rationale: barn owl (lechuza) chosen partly for IP distance —
  white heart face reads nothing like existing mascot owls.
- Dead ends / gotchas: branch names can't start with `dev/` while a branch
  named `dev` exists (git ref namespace conflict) — use `loop/*`/`chore/*`.
- Next suggested step: D brief in docs/MASCOT.md (placement map, pose/
  timing spec, palettes, copy lines, journey persistence decision).
