# 2026-07-19 — M25.4: 📲 install UX + M25 close (loop/20260719-m25-install-ux)

## What

Final M25 (PWA) iteration — the install affordance and milestone close:

- **☰ "📲 Instalar la app / Install"** (js/app.js `installMenuItem`):
  opens a small dialog on the info-overlay chrome. Where the browser
  captured `beforeinstallprompt` (Android/desktop Chrome — captured at
  boot with preventDefault), the panel shows a real "Instalar ahora"
  button that replays the prompt; otherwise per-platform steps — iOS
  (Compartir → "Añadir a pantalla de inicio", UA-detected) or generic
  browser-menu instructions. Focus-cycled dialog, Escape/backdrop close,
  and a ⬇️ Descargas link tying install → offline audio.
- **about.html** — new "Sin conexión / Offline" section (go-live content
  rule): installable, app cached on device, per-group audio via
  Descargas, everything stays on the device.
- **README** — "📱 Installable & offline" feature bullet.
- **docs/SPEC.md** — §5.5 retitled "designed and SHIPPED 2026-07-19".
- **GOAL.md** — INSTALL UX + V checked; M25 marked shipped (M25.1-M25.4,
  PRs #106-#109); queue line advanced: **M26 (stretch tenses) is now the
  ACTIVE milestone**, and the M28 note reflects the signed amendment
  (beacon code allowed; deploy waits on the vendor gate).

## Tests

- e2e: new M25.4 block — ☰ shows the install row; the panel opens with
  ≥2 steps and NO install-now button (no captured prompt in the
  harness); the Descargas link is present; Escape closes. Full suite
  green twice (with the M25.1/2/3 blocks all still passing).
- unit 60/60; payload budget green with the new panel code.

## M25 wrap-up

Four iterations, four auto-merged loop PRs, no CI reds. Owner actions
remaining (from the milestone entry): install-test on a real
iPhone/iPad, verify `?m18demo=1` on the live site post-SW, and merge
the rolling release PR to deploy it all.
