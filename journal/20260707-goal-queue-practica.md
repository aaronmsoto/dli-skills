# 2026-07-07 — GOAL.md queue + M8 decided (Práctica) + CLAUDE.md standing rules

**Branch:** `loop/20260707-goal-queue-practica` · **Type:** docs-only (no product code)

## Owner decisions captured

- **M8 decided**: Option 2 shape, named **🧩 Práctica**, and **unscored**
  (no stars, no badges, no result recording). A practice-based extension
  of the Estudia interface: rebuild the table via tap-tap matching, word
  by word, column by column, with vocalization per standard rules.
  Hold lifted; full acceptance criteria written; M8 is CURRENT.
- **Queue reordered**: M8 → M5 (reduced) → M9 → M10, recorded as an
  explicit queue line at the top of the Milestones section.
- **M4 paused indefinitely** (loops must not touch until reactivated).
- **M5 reduced** to what M10 does not cover: performance budget, SME copy
  review, printable layout tune-up.
- **M10 P-phase moved**: the Usability & Accessibility report page lives
  inside the public /docs section (`docs/usability.html`, linked from the
  hub) instead of a root `usability.html`.

## CLAUDE.md — new "Standing product rules" section

Persistent context distilled from recurring owner direction: (1)
vocalization everywhere it makes sense per standard TTS rules; (2)
Estudia links every activity, updated in the same PR as any activity
change; (3) every activity declares its scoring track (stars / badges /
unscored) and denominators are owner-only decisions; (4) kids appear
only as pseudonyms A1/A2; (5) standard chrome + single footer component
+ public pages stay unlinked until the owner links them; (6) the GOAL.md
queue line, not milestone numbers, sets loop priority.

## Validation

Docs-only; `npm test` green.

## Next

Implementation loop for M8 Práctica starts immediately after this merges.
