# loop: M7 🔍 Pistas (hint mode) · 2026-07-07 · loop/20260707-hint-mode

- Goal criteria addressed: first six M7 checkboxes (button+panel, scoping,
  no-penalty, is-hint pose, footer checkbox, e2e).
- What was done: makeHint(verb, tenses, lola) — 🔍 Pista button on prompt
  cards (Elige/Escribe/Contrast) toggling a mini Estudia panel (engine-
  generated column, vosotros-filtered; contrast shows BOTH past columns);
  panel resets each question; Lola gained a lens group in the SVG and the
  is-hint pose (lens to eye, slight head tilt, static under reduced
  motion); footer checkbox "🔍 Pistas / Hints" default CHECKED (settings
  key hints, backward-compatible); hidden from Escucha and Empareja.
- Validation: npm test 42/42 · e2e PASS with new checks: column equals the
  engine output for the verb in play, Lola raises/lowers the lens, panel
  resets on advance, contrast shows two tables, Escucha has no hint
  button, footer toggle removes and restores hints. Mobile 420px
  screenshot verified (panel fits under the prompt card).
- Decisions & rationale: closing the hint returns Lola to is-watching if
  the Escribe input is focused, else idle.
- Dead ends / gotchas: hint-table needs `.conj-table.hint-table { min-width: 0 }`
  or the study table's 640px min-width bleeds in.
- Next suggested step: final M7 criterion — Estudia screen must link every
  current activity (games + Escucha when voiced + Contrast on past tenses).
